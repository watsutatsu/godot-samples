on: [push]

jobs:
    fail:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - name: Run tests
              run: |
                  echo "Tests failed"
                  exit 1